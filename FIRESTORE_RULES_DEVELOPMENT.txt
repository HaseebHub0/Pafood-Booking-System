/**
 * FIRESTORE SECURITY RULES - DEVELOPMENT/TEST MODE
 * 
 * ⚠️ WARNING: These rules are LESS SECURE - Use only for development/testing
 * 
 * Database ID: pafood
 * Project ID: pakasianfood-field
 * 
 * Copy this entire code and paste in Firebase Console > Firestore Database > Rules
 * Then click "Publish" to deploy
 * 
 * This allows all authenticated users to read/write all data.
 * Use this only if you're having permission issues during development.
 */

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all authenticated users to read and write
      allow read, write: if request.auth != null;
    }
  }
}

